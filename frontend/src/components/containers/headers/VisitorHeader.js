import { Button, Nav, Navbar, Form, FormControl } from "react-bootstrap";
import HeaderWrapper from "./Header.styles";

export default function VisitorHeader({ style, className }) {
  return (
    <HeaderWrapper>
      <div style={style}>
        <button></button>
      </div>
    </HeaderWrapper>
  );
}
